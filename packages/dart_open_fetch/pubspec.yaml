name: dart_open_fetch
description: A standalone Dart CLI tool that generates typed HTTP clients from OpenAPI 3.0/3.1 schemas.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

resolution: workspace

executables:
  dart_open_fetch:

dependencies:
  args: ^2.4.0
  http: ^1.1.0
  yaml: ^3.1.0
  dart_open_fetch_core:

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
